[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "simulator"
version = "0.1.0"
description = "Config-driven Habitat-Sim engine shared across course homeworks"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["simulator*"]
